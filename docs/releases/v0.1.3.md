# Yuzuha v0.1.3

Status: Published on 2026-08-05.

## Android release

- Source commit: `41955c1`.
- Package: `dev.yuzuha`.
- Version name: `0.1.3`.
- Version code: `4`.
- APK: [`Yuzuha-0.1.3.apk`](https://github.com/Tsukugi/Yuzuha/releases/download/v0.1.3/Yuzuha-0.1.3.apk).
- GitHub Release: [Tsukugi/Yuzuha v0.1.3](https://github.com/Tsukugi/Yuzuha/releases/tag/v0.1.3).
- Size: `67,425,831` bytes.
- SHA-256: `02E1FEB0888EF1AE40FC0F6E25B56C47FF9DABFE4A1C2729164C672F2349BE82`.
- The APK uses the private non-debug Android release signer and carries the new
  OTA public key.

## OTA release

- The embedded/native baseline is `0.1.3`; the signed OTA bundle is `0.1.4`.
- GitHub Release: [Yuzuha OTA v0.1.4](https://github.com/Tsukugi/Yuzuha/releases/tag/v0.1.4).
- Metadata: [`bundle.json`](https://github.com/Tsukugi/Yuzuha/releases/download/v0.1.4/bundle.json).
- Bundle: [`Yuzuha-0.1.4.jsbundle`](https://github.com/Tsukugi/Yuzuha/releases/download/v0.1.4/Yuzuha-0.1.4.jsbundle).
- Bundle size: `1,320,896` bytes.
- Bundle SHA-256: `9d2442197f64e714073875ccec56537ba4bbc44b1acccc09b45a4141699ec98f`.
- The latest metadata URL returned HTTP 200 and pointed to version `0.1.4`
  with minimum native version `0.1.3`.
- `npm run ota-check` accepted the downloaded published metadata and the
  downloaded bundle matched its signed size and hash.

## Trust anchor

- The new Ed25519 public-key SHA-256 fingerprint is
  `cf181636d6a4ee260f4739a3239c1eb17dd72dcb2bd9c93ad5c74f59a7b28f5e`.
- The private key remains outside the repository in the local secret store.
- The APK was rebuilt after the pin changed; the old OTA key is not used by
  the current release.

## Verification evidence

- Full Jest: 53 suites, 251 tests passing.
- Native Kotlin unit tests: 15 tests passing.
- TypeScript, lint, current bundle validation, signed release build, and
  release APK installation passed.
- Xiaomi device `42adce68` first launched the published bundle with:
  `YuzuhaInstaller: launch kind=remote-activated version=0.1.4 reason=none`.
- The next launch promoted the healthy bundle and logged:
  `YuzuhaInstaller: launch kind=local-current version=0.1.4 reason=REMOTE_NOT_NEWER`.
- A temporary signed failing `0.1.5` prerelease was used for rollback smoke.
  Its first launch logged `remote-activated version=0.1.5`; the next launch
  selected `0.1.4` with `reason=BLOCKED_VERSION`. The temporary release and tag
  were deleted after the test.
- No product data was cleared during the OTA activation or rollback checks.

## Release boundary

This release keeps app schema 33 and repository schema 3. OTA code changes are
limited to the compatible React Native JavaScript bundle. Native changes,
permissions, schema changes, iOS OTA, sync, and background update workers still
require separate work and a new APK.
